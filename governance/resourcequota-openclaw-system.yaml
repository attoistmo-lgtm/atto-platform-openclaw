apiVersion: v1
kind: ResourceQuota
metadata:
  name: atto-platform-resource-quota
  namespace: openclaw-system
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 8Gi
    limits.cpu: "8"
    limits.memory: 16Gi
    pods: "10"
    persistentvolumeclaims: "2"
    requests.storage: 20Gi
